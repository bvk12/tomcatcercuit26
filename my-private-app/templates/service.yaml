apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-private-app.fullname" . }}
  labels:
    {{- include "my-private-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-private-app.selectorLabels" . | nindent 4 }}
